@{
    ViewData["Title"] = "Administrador";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Administrador</h2>

@(Html.DevExtreme().DataGrid<ProyectoLinkedinMVC.Models.Administrador>()
    .DataSource(ds => ds.WebApi()
        .Controller("Administrador")
        .LoadAction("Get")
        .InsertAction("Post")
        .UpdateAction("Put")
        .DeleteAction("Delete")
        .Key("Id")
    )
    .RemoteOperations(true)
    .Columns(columns => {

        columns.AddFor(m => m.Clave);

        columns.AddFor(m => m.Id);

        columns.AddFor(m => m.Nombre);

        columns.AddFor(m => m.Apellido);

        columns.AddFor(m => m.Telefono);

        columns.AddFor(m => m.Correo);
    })
    .Editing(e => e
        .AllowAdding(true)
        .AllowUpdating(true)
        .AllowDeleting(true)
    )
)
