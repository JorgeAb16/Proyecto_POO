@{
    ViewData["Title"] = "Experiencia";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2 class="text-center text-primary mb-4">Experiencia</h2>

@(Html.DevExtreme().DataGrid<ProyectoLinkedinMVC.Models.Experiencia>()
    .DataSource(ds => ds.WebApi()
        .Controller("Experiencia")
        .LoadAction("Get")
        .InsertAction("Post")
        .UpdateAction("Put")
        .DeleteAction("Delete")
        .Key("Id")
    )
// Configuración Responsive y de Estilo
.Width("100%")
.ColumnAutoWidth(true)
.AllowColumnResizing(true)
.ColumnMinWidth(120)
.ColumnResizingMode(ColumnResizingMode.Widget)
.ShowBorders(true)
.HoverStateEnabled(true)
.ShowColumnLines(true)
.ShowRowLines(true)
.RowAlternationEnabled(true)
.WordWrapEnabled(true)

// Configuración de Operaciones
.RemoteOperations(true)
.Scrolling(s => s.Mode(GridScrollingMode.Virtual))
.ColumnHidingEnabled(true)

// Configuración de Filtrado y Búsqueda
.FilterRow(f => f.Visible(true).ApplyFilter(GridApplyFilterMode.Auto))
.HeaderFilter(f => f.Visible(true))
.FilterPanel(f => f.Visible(true))
.SearchPanel(f => f.Visible(true).Width(300).Placeholder("Buscar..."))

// Configuración de Agrupación
.GroupPanel(p => p.Visible(true))
.Grouping(g => g.AutoExpandAll(false))

// Configuración de Paginación
.Paging(p => p.PageSize(10))
.Pager(p => p
    .ShowPageSizeSelector(true)
    .AllowedPageSizes(new[] { 5, 10, 20 })
    .ShowInfo(true)
    .ShowNavigationButtons(true))
    .Columns(columns => {

        columns.AddFor(m => m.Id);

        columns.AddFor(m => m.Cargo);

        columns.AddFor(m => m.Fecha_inicio);

        columns.AddFor(m => m.Fecha_fin);

        columns.AddFor(m => m.Empresa);

        columns.AddFor(m => m.UsuarioID);

        columns.Add()
        .DataField("UsuarioNombre")
        .Caption("Nombre de Usuario");
    })
                .Editing(e => e
        .Mode(GridEditMode.Popup)
        .AllowAdding(true)
        .AllowUpdating(true)
        .AllowDeleting(true)
        .UseIcons(true)
        .Popup(p => p
            .Title("Editar Registro")
            .Width("70%")
            .Height("auto")
        )
    )
    .OnInitialized("adjustGridLayout")
)
