@{
    ViewData["Title"] = "Notificacion";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2 class="text-center text-primary mb-4">Gestión de Notificaciones</h2>

<style>
    .dx-datagrid-text-content {
        font-weight: bold !important;
    }

    .dx-datagrid-action {
        text-align: center !important;
    }
</style>

@(Html.DevExtreme().DataGrid<ProyectoLinkedinMVC.Models.Notificacion>()
    .DataSource(ds => ds.WebApi()
        .Controller("Notificacion")
        .LoadAction("Get")
        .InsertAction("Post")
        .UpdateAction("Put")
        .DeleteAction("Delete")
        .Key("Id")
    )
    // Configuración Responsive y de Estilo
    .Width("100%")
    .ColumnAutoWidth(true)
    .AllowColumnResizing(true)
    .ColumnMinWidth(120)
    .ColumnResizingMode(ColumnResizingMode.Widget)
    .ShowBorders(true)
    .HoverStateEnabled(true)
    .ShowColumnLines(true)
    .ShowRowLines(true)
    .RowAlternationEnabled(true)
    .WordWrapEnabled(true)

    // Configuración de Operaciones
    .RemoteOperations(true)
    .Scrolling(s => s.Mode(GridScrollingMode.Virtual))
    .ColumnHidingEnabled(true)

    // Configuración de Filtrado y Búsqueda
    .FilterRow(f => f.Visible(true).ApplyFilter(GridApplyFilterMode.Auto))
    .HeaderFilter(f => f.Visible(false))
    .FilterPanel(f => f.Visible(true))
    .SearchPanel(f => f.Visible(true).Width(300).Placeholder("Buscar..."))

    // Configuración de Agrupación
    .GroupPanel(p => p.Visible(false))
    .Grouping(g => g.AutoExpandAll(false))

    // Configuración de Paginación
    .Paging(p => p.PageSize(10))
    .Pager(p => p
        .ShowPageSizeSelector(true)
        .AllowedPageSizes(new[] { 5, 10, 20 })
        .ShowInfo(true)
        .ShowNavigationButtons(true))
    .Columns(columns => {

        columns.AddFor(m => m.Id);

        columns.AddFor(m => m.Mensaje);

        columns.AddFor(m => m.Fechaenvio);

        columns.AddFor(m => m.DestinatarioId);
        columns.Add()
.DataField("Destinatario")
.Caption("Nombre del Destinatario");
    })
    .Editing(e => e
        .Mode(GridEditMode.Popup)
        .AllowAdding(true)
        .AllowUpdating(true)
        .AllowDeleting(true)
        .UseIcons(true)
        .Popup(p => p
            .Title("Editar Registro")
            .Width("70%")
            .Height("auto")
        )
    )
    .OnInitialized("adjustGridLayout")
)
